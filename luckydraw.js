var config = {
    type: Phaser.AUTO,
    width: 800,
    height: 600,
    scene: {
        preload: preload,
        create: create,
        update: update
    }
};

var game = new Phaser.Game(config);
var degree = 360;
function preload ()
{
}

function create ()
{
}

function update ()
{
}

function drawCircle(listOfItem){

  
}
